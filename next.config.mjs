/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'encrypted-tbn0.gstatic.com',
      'pic2.zhimg.com',
      'uhmgreyhats.github.io',
    ],
  },
};

export default nextConfig;